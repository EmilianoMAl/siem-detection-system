# Dependencias completas del proyecto (motor de detección + dashboard + tests).
# Para desplegar solo el dashboard puedes usar dashboard/requirements.txt.
streamlit>=1.44.0
plotly>=5.22.0
pandas>=2.2.2
pyyaml>=6.0

# Tests
pytest>=8.0.0
